#Packages
library(blogdown)
library(pacman)
library(BiocManager)

#Bash commands to connect local directory and version control repo

# cd <local directory of R project>
# git init
# git add.
# git commit - m "first commit message"
# git branch -M origin
# git remote add origin <link to repo>
# git push -u origin main